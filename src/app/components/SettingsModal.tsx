/**
 * Settings modal entry — keeps stable import path for App.tsx lazy load.
 * Implementation: left nav + right content via shadcn Dialog (settings-modal-refactor).
 */
export { SettingsShell as SettingsModal } from "./settings/SettingsShell";
