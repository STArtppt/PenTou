# Dependencies
node_modules/
extension/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
dist/
dist-server/
extension/dist/
# Extension Chrome Web Store upload package (rebuild via browser-extension guide §9.2)
extension/*.zip
# Local store listing screenshots (not source; upload only to CWS console)
extension/screenshot*.png
tests/
vite-plugins/**/*.test.ts
*.tsbuildinfo

# Vite
.vite/
vite.config.*.timestamp-*

# Environment variables
.env
.env.local
.env.*.local
.env.production
.env.development

# Local data
data/conversations/*
data/documents/*
data/.session-secret
data/document-folders.json
data/folders.json
data/ai-chats/*
data/assets/*
# Search index (derived from .md, rebuildable)
data/.qmd/
.qmd/
# Embedding backend config (contains API key)
data/.config/
.config/
# Ingest token & config (token is a secret — spec ingest-gateway §4.3)
data/ingest/
# Session signing secret (per-machine secret, auto-regenerated by getOrCreateSessionSecret)
data/.session-secret
# Per-machine local UI state (folder tree); tracked historically on main, aligned with public branch
data/folders.json
data/document-folders.json
# npx-launcher default runtime data dir (<cwd>/pentou-data): real conversations + .session-secret.
# Accidentally committed in abf7e4f while smoke-testing from repo root — never track.
pentou-data/

# IDE and editors
.idea/
.obsidian/
.claude/
.trae/
/.drafts/
*.swp
*.swo
*~
.DS_Store
*.sublime-project
*.sublime-workspace
.pnpm-store/
.grok/

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Logs
logs/
*.log

# Temporary files
*.tmp
*.temp
.cache/

# Coverage reports
coverage/
*.lcov
.nyc_output/

# TypeScript
*.tsbuildinfo

# Optional npm cache directory
.npm
.npm-cache/

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache
.parcel-cache

# Next.js
.next/
out/

# Nuxt.js
.nuxt/

# Storybook build outputs
storybook-static/

# Temporary folders
tmp/
temp/

# Git version management temporary files
.git-versions/

# Database files (keep directory structure and initial data)
# assets/database/*.json
# !assets/database/.gitkeep

.waylog/

# npx package staging directory
npm-package/

# python3 脚本执行时生成的缓存文件
__pycache__/
*.py[cod]