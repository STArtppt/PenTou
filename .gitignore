# Dependencies
node_modules/
extension/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
dist/
dist-server/
extension/dist/
# Extension Chrome Web Store upload package (rebuild via browser-extension guide §9.2)
extension/*.zip
# Local store listing screenshots (not source; upload only to CWS console)
extension/screenshot*.png
tests/
vite-plugins/**/*.test.ts
*.tsbuildinfo

# Vite
.vite/
vite.config.*.timestamp-*

# Environment variables
.env
.env.local
.env.*.local
.env.production
.env.development

# Local data
data/conversations/*
data/documents/*
data/.session-secret
data/document-folders.json
data/folders.json
data/ai-chats/*
data/assets/*
assets/private/*
# Search index (derived from .md, rebuildable)

data/.qmd/
.qmd/
# Embedding backend config (contains API key)
data/.config/
.config/
# Ingest token & config (token is a secret — spec ingest-gateway §4.3)
data/ingest/
# Session signing secret (per-machine secret, auto-regenerated by getOrCreateSessionSecret)
data/.session-secret
# Per-machine local UI state (folder tree); tracked historically on main, aligned with public branch
data/folders.json
data/document-folders.json
# 同类：文档项目清单（spec document-projects），随本机采集/手建产生，不入库
data/document-projects.json
# npx-launcher default runtime data dir (<cwd>/pentou-data): real conversations + .session-secret.
# Accidentally committed in abf7e4f while smoke-testing from repo root — never track.
pentou-data/

# IDE and editors
.idea/
.obsidian/
# .claude/ 混着三类内容，按性质切：
#   生成物（skills/openspec-*、commands/opsx/*，由 `openspec init --tools claude` 产出）→ 不跟，避免升级 diff 噪音
#   settings.local.json（本机权限记录）→ 不跟
#   settings.json（团队共享 hooks / 权限规则，有人类决策含量）→ 跟
.claude/*
!.claude/settings.json
.trae/
/.drafts/
*.swp
*.swo
*~
.DS_Store
*.sublime-project
*.sublime-workspace
.pnpm-store/
# 同 .claude/：.grok/skills/ 是手写的 Grok 入口薄壳（真源在 skills/），跟；其余会话/缓存不跟。
.grok/*
!.grok/skills/

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Logs
logs/
*.log

# Temporary files
*.tmp
*.temp
.cache/

# Coverage reports
coverage/
*.lcov
.nyc_output/

# TypeScript
*.tsbuildinfo

# Optional npm cache directory
.npm
.npm-cache/

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache
.parcel-cache

# Next.js
.next/
out/

# Nuxt.js
.nuxt/

# Storybook build outputs
storybook-static/

# Temporary folders
tmp/
temp/

# Git version management temporary files
.git-versions/

# Database files (keep directory structure and initial data)
# assets/database/*.json
# !assets/database/.gitkeep

.waylog/

# npx package staging directory
npm-package/

# python3 脚本执行时生成的缓存文件
__pycache__/
*.py[cod]